# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP.NET Core service.

version: '3.4'

services:
  pockeycloakapi:
    image: pockeycloakapi
    build:
      context: .
      dockerfile: src/poc.keycloak.api/Dockerfile
    ports:
      - 5264:5264
